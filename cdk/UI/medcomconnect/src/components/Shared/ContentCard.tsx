import Link from "next/link"; export default function ContentCard({ badge, title, description, href, cta, image }: { badge?: string; title: string; description: string; href: string; cta?: string; image?: string; }) { return ( <Link href={href} className="group block tf-card h-full flex flex-col"> {image && ( <div className="tf-card-img-placeholder" style={{ height: "180px", position: "relative", padding: 0 }}> <img src={image} alt={title} style={{ width: "100%", height: "100%", objectFit: "cover" }} /> </div> )} <div className="tf-card-body flex flex-col flex-grow"> {badge && <span className="text-xs font-medium uppercase tracking-wider mb-2" style={{ color: "var(--teal-dark)" }}>{badge}</span>} <h3 className="font-semibold text-lg mb-2" style={{ color: "var(--brown-dark)" }}> {title} </h3> <p className="text-sm leading-relaxed mb-6 flex-grow" style={{ color: "var(--text-muted)" }}> {description} </p> <span className="learn-more-link text-sm mt-auto"> {cta || "Learn more"} &nbsp;? </span> </div> </Link> ); }
